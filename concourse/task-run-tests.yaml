platform: linux

inputs:
  - name: code

outputs:
  - name: coverage-reports

params:
  CODECOV_TOKEN:

image_resource:
  type: docker-image
  source:
    repository: node:12-alpine

caches:
  - path: code/vendor

run:
  path: code/concourse/tests.sh
